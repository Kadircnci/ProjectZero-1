//
//  ContentView.swift
//  ProjectZero-1
//
//  Created by Kadir Çanakcı on 9.01.2025.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("What's up Mann This is an new commit in and ")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
